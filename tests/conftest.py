"""Shared pytest fixtures for streamwright tests.

Module-specific fixtures (e.g. the scheduler clock and queue-size
fixtures) are added alongside those modules as they land, to keep the
test suite importable and type-clean at every step of the build.
"""

from __future__ import annotations
